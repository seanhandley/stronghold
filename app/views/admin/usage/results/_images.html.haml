- if images && images.any?
  %div.usage-back
    %h3 Image Usage
    .table-responsive
      %table.table.table-hover.phone-bill
        %thead
          %tr
            %th.icon
              %i.fa.fa-upload
            %th Name
            %th Unique Identifier
            %th Created At
            %th Last Recorded Size
            %th Deleted At
            %th
              %i.fa.fa-clock-o
              Terabyte Hours
            - unless DarkLaunch.hide?
              %th
                %i.fa.fa-gbp
                Total
        %tbody
          - images.each do |image|
            %tr
              %td &nbsp;
              %td= image[:name]
              %td= image[:id]
              %td= image[:created_at]
              %td
                = image[:latest_size].round(2)
                GB
              %td= image[:deleted_at] ? image[:deleted_at] : '-'
              %td
                = number_with_delimiter(image[:terabyte_hours], :delimiter => ',')
                TBh
              - unless DarkLaunch.hide?
                %td== £#{number_with_precision((image[:terabyte_hours] * RateCard.block_storage).round(2), :delimiter => ',', :separator => '.', :precision => 2)}
          %tr.highlight.bottomline
            %td &nbsp;
            %td Total:
            %td
              = number_with_delimiter(images.collect{|v| v[:terabyte_hours]}.sum.round(2), :delimiter => ',')
              TBh
            - unless DarkLaunch.hide?
              %td
                == £#{number_with_precision((images.collect{|v| v[:terabyte_hours]}.sum * RateCard.block_storage).round(2), :delimiter => ',', :separator => '.', :precision => 2)}
            %td &nbsp
            %td &nbsp
            %td &nbsp
            %td &nbsp
