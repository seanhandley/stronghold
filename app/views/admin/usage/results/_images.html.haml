- if image_results
  %h2 Image Usage
  - image_results.each do |tenant, images|
    %h3= tenant
    - if images.values.any?
      %table.table.table-hover
        %thead
          %tr
            %th Name
            %th Terabyte Hours
        %tbody
          - images.each do |_, results|
            %tr
              %td= results[:name]
              %td= number_with_delimiter(results[:terabyte_hours], :delimiter => ',')
    - else
      No image data for this period.