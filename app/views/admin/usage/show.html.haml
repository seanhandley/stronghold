- title "Usage Data"

%h3
  Usage Data:
.usage-form
= form_tag(admin_usage_path(@organization), 'role' => 'form', 'method' => 'put') do
  .form-group
    %label From Date
    = datetime_select :from, nil, default: @from_date, class: 'form-control', minute_step: 15, min: Date.today
  .form-group
    %label To Date
    = datetime_select :to, nil, default: @to_date, class: 'form-control', minute_step: 15, max: DateTime.now

  .form-group
    %label Format
    = select_tag :format, options_for_select(['HTML (default)', 'CSV','JSON','XML'], selected: 'HTML (default)'), class: 'form-control'

  .form-group
    %label
      = check_box_tag :clear_cache, nil
      Clear Cached Results?
    %p.help-block Slower, but will regenerate data from scratch

  = submit_tag 'Generate →', class: 'btn btn-primary', onchange: "checkDate();"
