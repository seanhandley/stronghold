- title "Usage Data"
.container.usage-show-container
  = render partial: 'admin/customers/header', locals: { organization: @organization }
.usage-form
  %h3
    Usage Data:
  = form_tag(admin_usage_path(@organization), 'role' => 'form', 'method' => 'put') do
    .form-group
      %label From Date
      = datetime_select :from, nil, default: @from_date, class: 'form-control', minute_step: 15
    .form-group
      %label To Date
      = datetime_select :to, nil, default: @to_date, class: 'form-control', minute_step: 15

    .form-group
      %label Format
      = select_tag :format, options_for_select(['HTML (default)', 'CSV','JSON','XML'], selected: 'HTML (default)'), class: 'form-control'

    .form-group
      %label
        = check_box_tag :clear_cache, nil
        Clear Cached Results?
      %p.help-block Slower, but will regenerate data from scratch

    = submit_tag 'Generate →', class: 'btn btn-primary'
