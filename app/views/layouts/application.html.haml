!!!
%html{'ng-app' => "angularJS"}
  %head
    %title= content_for?(:title) ? content_for(:title) : "DataCentred"
    = render partial: 'shared/preconnects'
    = render partial: 'shared/javascript_includes'
    = csrf_meta_tags

%body
  = render partial: 'shared/stylesheet_includes'
  = render partial: 'shared/google_tags'
  #wrap
    .top-nav.navbar.navbar-default.navbar-fixed-top
      .container{style: "width:100%"}
        = render partial: 'shared/mob_menu'
        %a{href: support_root_path, class: 'back-to-dash' }
          %h1.logo
        = render partial: 'shared/top_menu'
        .context-nav
          = render partial: 'shared/support_nav'
      
    %div.page-body
      = render partial: 'shared/environment'
      = render partial: 'shared/cloud_services'
      .container
        - if request.url == support_root_url || activated_url
          = render :partial => "announcements/starburst/announcement_bootstrap"
        %p &nbsp;
        = display_flash
        = yield
        .push
= render partial: 'shared/footer' if current_user.organization.known_to_payment_gateway?
= render partial: 'shared/ga'
#loading-overlay.hide
  .spinner
    %i.fa.fa-cog.fa-spin