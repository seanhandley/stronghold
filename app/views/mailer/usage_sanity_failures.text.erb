The following items are active in the usage data but no longer present on OpenStack.

<% for key in @keys %>
  <% if @data["missing_#{key}".to_sym].present? %>
    <%= key.titleize %>

    <% @data["missing_#{key}".to_sym].each do |k,v| %>
      <%= "#{v} (#{k})" %>
    <% end %>
   
  <% end %>
<% end %>